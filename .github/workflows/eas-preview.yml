name: 'Preview eas'
on:
  pull_request:
jobs:
   preview-eas:
    uses: bilwifi/gh-action-test/.github/workflows/generic-eas-preview.yml@main
    secrets: inherit
    with:
      branch: ${{ github.event.pull_request.head.ref }}
