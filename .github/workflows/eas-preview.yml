name: 'Preview eas'
on:
  pull_request:
    branches:
      - main
    types:
      - open
      #- reopen
jobs:
   generic-eas-preview:
    uses: bilwifi/gh-action-test/.github/workflows/generic-eas-preview.yml@main
    secrets: inherit
