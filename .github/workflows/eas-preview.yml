name: 'Preview eas'
on:
  pull_request:
    branches:
      - main
    types:
      - open
jobs:
   generic-eas-preview:
    uses: github.com/bilwifi/gh-action-test/.github/workflows/{generic-eas-preview.yml}@{main}
    secrets: inherit
